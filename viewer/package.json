{
    "name": "elm-review-call-graph",
    "scripts": {
        "run": "npm-run-all --print-name --silent --sequential prepare watch",
        "prepare": "mkdir -p build; cp src/index.html build/;",
        "watch": "yarn elm-watch hot"
    },
    "dependencies": {},
    "devDependencies": {
        "elm-watch": "^2.0.0-beta.3",
        "npm-run-all": "^4.1.5"
    },
    "license": "BSD-3-Clause",
    "overrides": {
        "elm-doc-preview": ">=6.0.0",
        "cross-spawn": ">=7.0.3"
    },
    "resolutions": {
        "elm-doc-preview": ">=6.0.0",
        "cross-spawn": ">=7.0.3"
    }
}
